# Leave empty in dev — Vite proxies /predict, /health, /reports to port 8000
# In production behind nginx, also leave empty (same-origin proxy)
# For direct API URL: http://localhost:8000
VITE_API_URL=
